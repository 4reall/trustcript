import useBreakpointValues from './useBreakpointValues';

const useBreakpointProps = <T>(props: T): T => {
	// const {} = useBreakpointValues<T>();
	return props;
};
export default useBreakpointProps;
