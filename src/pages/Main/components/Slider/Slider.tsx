import { SliderContainer } from 'pages/Main/components/Slider/Slider.styles';
import Carousel from 'components/Carousel/Carousel';

const Slider = () => {
	return (
		<SliderContainer>
			<Carousel />
		</SliderContainer>
	);
};

export default Slider;
